fn main() {
    let condition = true;

    // `if` is an expression in Rust, so you can assign it to a variable
    let number = if condition { 5 } else { 6 };

    println!("The value of number is: {}", number);
}
